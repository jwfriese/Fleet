language: swift
os: osx
osx_image: xcode11.3
branches:
  only:
  - master
install:
  - bundle install
  - carthage update --platform 'iOS' 
  - carthage update --platform 'tvOS' 
script:
  - fastlane test
